# AI Accountant v2 — backend dependencies
# Pinned to the versions that are known-good in the project venv.
# Rebuild the venv with:  python -m venv venv && venv/bin/pip install -r requirements.txt

# Web framework
fastapi==0.139.0
uvicorn==0.51.0
starlette==1.3.1
python-multipart==0.0.9
pydantic==2.13.4

# Google AI / auth (Google OAuth token verification)
google-auth==2.55.1
google-api-core==2.31.0
googleapis-common-protos==1.75.0
google-crc32c==1.8.0
requests==2.34.2

# Document parsing
pdfplumber==0.11.0
pdfminer.six==20231228
pandas==2.2.1
openpyxl==3.1.2
numpy==1.26.4
numpy-financial==1.0.0

# Misc helpers used across agents/services
rapidfuzz==3.14.5
tenacity==9.1.4
python-dotenv==1.2.2
httpx==0.28.1
Pillow==12.3.0
aiosqlite==0.22.1
# NOTE: pywebview is intentionally NOT listed here. It is a native GUI toolkit
# that requires a display server and only works on the local desktop app
# (backend/gui_launcher.py). On Render/container platforms there is no display,
# so leaving it in breaks `pip install`. The backend import path never pulls it,
# so removing it is safe for both local and cloud deployments.
